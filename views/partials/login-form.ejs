<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <form method="POST" action="/login">
        <div class="self-center text-3xl font-bold text-blue-500">Login</div>
        <div class="w-full flex flex-col gap-2">
          <label for="email">Email</label>
          <input type="email" id="email" name="email" placeholder="Email" class="border border-black rounded-xl" required />
        </div>
        <div class="w-full flex flex-col gap-2">
          <label for="password">Password</label>
          <input type="password" id="password" name="password" placeholder="Password" class="border border-black rounded-xl" required />
        </div>
        <button type="submit" class="text-white w-full cursor-pointer bg-blue-500 rounded-xl py-2 mt-4">Login</button>
        <div class="self-end mt-2">
          <a href="/forgot-password/email" class="text-sm text-red-500">Forgot Password?</a>
        </div>
        <div class="self-start mt-4">
          <div class="flex gap-2">Don't have an account?
            <span class="text-blue-500 font-semibold">
              <a href="/register">Register</a>
            </span>
          </div>
        </div>
        
    <!-- Google Login Button -->
    <div class="self-center mt-4">
      <a href="/auth/google" class="text-white bg-red-500 rounded-xl py-2 px-4">
          Login with Google
      </a>
  </div>
      </form>
</body>
</html>